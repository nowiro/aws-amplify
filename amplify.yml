version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        - yarn run test:ci
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: dist/mf-host
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*

